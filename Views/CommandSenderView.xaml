<UserControl x:Class="MqttMonitor.Views.CommandSenderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="Transparent">
    <!-- 资源 -->
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <!-- 发送按钮样式 -->
        <Style x:Key="SendButton" TargetType="Button">
            <Setter Property="Background" Value="#28A745"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#218838"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#1E7E34"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 清空按钮样式 -->
        <Style x:Key="ClearButton" TargetType="Button">
            <Setter Property="Background" Value="#DC3545"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#C82333"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#BD2130"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 命令发送区 -->
        <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="20" Margin="0,0,0,15"
                BorderBrush="#E0E0E0" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect Color="#000000" Opacity="0.05" BlurRadius="10" ShadowDepth="2"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="110"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- 标题 -->
                <TextBlock Grid.Row="0" Grid.ColumnSpan="2"
                          Text="命令发送" FontSize="16" FontWeight="SemiBold"
                          Foreground="#212529" Margin="0,0,0,20"/>

                <!-- 设备ID -->
                <TextBlock Grid.Row="1" Grid.Column="0"
                          Text="目标设备:" VerticalAlignment="Center"
                          Foreground="#495057" FontSize="13" Margin="0,0,10,12"/>
                <TextBox Grid.Row="1" Grid.Column="1"
                        Text="{Binding SelectedDeviceId, UpdateSourceTrigger=PropertyChanged}"
                        Height="36" Margin="0,0,0,12"/>

                <!-- 命令名称 -->
                <TextBlock Grid.Row="2" Grid.Column="0"
                          Text="命令名称:" VerticalAlignment="Center"
                          Foreground="#495057" FontSize="13" Margin="0,0,10,12"/>
                <TextBox Grid.Row="2" Grid.Column="1"
                        Text="{Binding CommandName, UpdateSourceTrigger=PropertyChanged}"
                        Height="36" Margin="0,0,0,12"/>

                <!-- 命令参数 -->
                <TextBlock Grid.Row="3" Grid.Column="0"
                          Text="命令参数:" VerticalAlignment="Top"
                          Foreground="#495057" FontSize="13" Margin="0,5,10,12"/>
                <TextBox Grid.Row="3" Grid.Column="1"
                        Text="{Binding CommandParams, UpdateSourceTrigger=PropertyChanged}"
                        TextWrapping="Wrap" AcceptsReturn="True"
                        Height="90" Margin="0,0,0,12"
                        VerticalScrollBarVisibility="Auto"
                        Padding="8"/>

                <!-- 参数说明 -->
                <TextBlock Grid.Row="4" Grid.Column="1"
                          Text="输入命令参数（纯文本字符串）"
                          TextWrapping="Wrap" Foreground="#6C757D" FontSize="11"
                          Margin="0,0,0,15"/>

                <!-- 发送按钮 -->
                <Button Grid.Row="5" Grid.Column="1"
                       Content="发送命令" Command="{Binding SendCommandCommand}"
                       Width="130" HorizontalAlignment="Left" Height="36"
                       Style="{StaticResource SendButton}"/>
            </Grid>
        </Border>

        <!-- 历史记录区 -->
        <Grid Grid.Row="1" Grid.RowSpan="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 历史记录标题栏 -->
            <Border Grid.Row="0" Background="White" Padding="15,12" CornerRadius="8,8,0,0"
                    BorderBrush="#E0E0E0" BorderThickness="1,1,1,0">
                <Grid>
                    <TextBlock Text="命令历史记录" FontSize="15" FontWeight="SemiBold"
                              Foreground="#212529" VerticalAlignment="Center"/>
                    <Button Content="清空记录" Command="{Binding ClearHistoryCommand}"
                           HorizontalAlignment="Right" Width="90" Height="32"
                           Style="{StaticResource ClearButton}"/>
                </Grid>
            </Border>

            <!-- 历史记录列表 -->
            <Border Grid.Row="1" Background="White" CornerRadius="0,0,8,8"
                    BorderBrush="#E0E0E0" BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect Color="#000000" Opacity="0.05" BlurRadius="10" ShadowDepth="2"/>
                </Border.Effect>
                <DataGrid ItemsSource="{Binding CommandHistory}"
                         AutoGenerateColumns="False"
                         IsReadOnly="True"
                         SelectionMode="Single"
                         CanUserResizeRows="False"
                         HeadersVisibility="Column"
                         GridLinesVisibility="Horizontal"
                         AlternatingRowBackground="#F8F9FA"
                         Background="White"
                         BorderThickness="0"
                         RowHeight="35"
                         ColumnHeaderHeight="40"
                         HorizontalGridLinesBrush="#E9ECEF">
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F8F9FA"/>
                            <Setter Property="Foreground" Value="#495057"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="FontSize" Value="12"/>
                            <Setter Property="Padding" Value="12,8"/>
                            <Setter Property="BorderBrush" Value="#DEE2E6"/>
                            <Setter Property="BorderThickness" Value="0,0,1,1"/>
                            <Setter Property="HorizontalContentAlignment" Value="Left"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="12,8"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="FontSize" Value="12"/>
                            <Setter Property="Foreground" Value="#212529"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#E7F3FF"/>
                                    <Setter Property="Foreground" Value="#212529"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.CellStyle>
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#F1F3F5"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="时间" Binding="{Binding Time, StringFormat='yyyy-MM-dd HH:mm:ss'}" Width="150"/>
                        <DataGridTextColumn Header="设备ID" Binding="{Binding DeviceId}" Width="150"/>
                        <DataGridTextColumn Header="命令名称" Binding="{Binding CommandName}" Width="150"/>
                        <DataGridTextColumn Header="命令ID" Binding="{Binding CommandId}" Width="200"/>
                        <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="*"/>
                        <DataGridTextColumn Header="响应时间" Binding="{Binding ResponseTime, StringFormat='yyyy-MM-dd HH:mm:ss'}" Width="150"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
