<Window x:Class="MqttMonitor.Views.ConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{DynamicResource Confirm.Title}"
        Height="220" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    <Border Background="White" CornerRadius="8" BorderBrush="#007BFF" BorderThickness="2" Margin="10">
        <Border.Effect>
            <DropShadowEffect Color="#000000" Opacity="0.3" BlurRadius="15" ShadowDepth="5"/>
        </Border.Effect>
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title -->
            <TextBlock Grid.Row="0" Text="{DynamicResource Confirm.Title}"
                       FontSize="18" FontWeight="Bold" Foreground="#212529"
                       Margin="0,0,0,20"/>

            <!-- Message -->
            <Border Grid.Row="1" Background="#F0F7FF" BorderBrush="#B3D9FF" BorderThickness="1"
                    CornerRadius="4" Padding="15" Margin="0,0,0,20">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="⚠️" FontSize="24" VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <TextBlock x:Name="MessageTextBlock" TextWrapping="Wrap" FontSize="14"
                               Foreground="#212529" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Buttons -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="YesButton" Content="{DynamicResource Confirm.Yes}"
                        Width="90" Height="35" Margin="0,0,10,0"
                        Style="{StaticResource DangerButton}"
                        Click="YesButton_Click"/>
                <Button x:Name="NoButton" Content="{DynamicResource Confirm.No}"
                        Width="90" Height="35"
                        Style="{StaticResource SecondaryButton}"
                        Click="NoButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
